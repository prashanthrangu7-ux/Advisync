# Advisync

Webpage for Advisync.

## Vercel Groq setup

The chat API expects the Groq key to be stored as a Vercel environment variable named `GROQ_API_KEY`. You can optionally set `GROQ_MODEL` to override the default `llama-3.3-70b-versatile` model.
